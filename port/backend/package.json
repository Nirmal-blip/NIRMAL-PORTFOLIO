{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Backend API for portfolio contact form",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Backend build completed - no compilation needed for Node.js' && npm ci --omit=dev",
    "build:clean": "rm -rf node_modules package-lock.json && npm install --production"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": ["portfolio", "contact", "email", "nodemailer"],
  "author": "Your Name",
  "license": "MIT"
}
